% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdp.R
\name{cdp}
\alias{cdp}
\title{Confirmed Disability Progression (CDP)}
\usage{
cdp(
  dt,
  idvar,
  edssvar,
  datevar,
  dxdate,
  confirmdays = 180L,
  baselinewindow = 730L,
  roving = FALSE,
  allevents = FALSE
)
}
\arguments{
\item{dt}{A data.frame or data.table with EDSS measurements (long format).}

\item{idvar}{Name of the person ID column.}

\item{edssvar}{Name of the EDSS score column (numeric).}

\item{datevar}{Name of the measurement date column (Date class).}

\item{dxdate}{Name of the diagnosis date column (Date class).}

\item{confirmdays}{Days required for confirmation (default 180).}

\item{baselinewindow}{Days from diagnosis to search for baseline EDSS (default 730).}

\item{roving}{Logical; if TRUE, use roving baseline (reset after each confirmed
progression). Default FALSE.}

\item{allevents}{Logical; if TRUE and roving=TRUE, track all CDP events not
just first. Default FALSE.}
}
\value{
A list with \verb{$data} (data.table with one row per person/event with
\code{cdp_date}) and \verb{$info} containing N_persons, N_events, confirmdays,
baselinewindow.
}
\description{
Identifies confirmed disability progression events from EDSS measurements.
Baseline EDSS is determined from the first measurement within a window of
diagnosis. Progression requires a sustained increase above threshold.
}
\examples{
edss <- data.table::data.table(
  pid = rep("A", 4),
  edss = c(2.0, 3.5, 3.5, 3.5),
  date = as.Date(c("2010-01-01", "2011-06-01", "2011-09-01", "2012-01-01")),
  dx = as.Date("2009-06-01")
)
cdp(edss, idvar = "pid", edssvar = "edss", datevar = "date", dxdate = "dx")
}
