% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pira.R
\name{pira}
\alias{pira}
\title{Progression Independent of Relapse Activity (PIRA)}
\usage{
pira(
  dt,
  idvar,
  edssvar,
  datevar,
  dxdate,
  relapses,
  relapseidvar = NULL,
  relapsedatevar = "relapse_date",
  windowbefore = 90L,
  windowafter = 30L,
  confirmdays = 180L,
  baselinewindow = 730L,
  rebaselinerelapse = FALSE
)
}
\arguments{
\item{dt}{A data.frame or data.table with EDSS measurements (long format).}

\item{idvar}{Name of the person ID column.}

\item{edssvar}{Name of the EDSS score column (numeric).}

\item{datevar}{Name of the measurement date column (Date class).}

\item{dxdate}{Name of the diagnosis date column (Date class).}

\item{relapses}{A data.frame or data.table with relapse data containing
id and relapse date columns.}

\item{relapseidvar}{Name of the ID column in relapses (default: same as idvar).}

\item{relapsedatevar}{Name of the relapse date column (default: "relapse_date").}

\item{windowbefore}{Days before relapse to exclude (default 90).}

\item{windowafter}{Days after relapse to exclude (default 30).}

\item{confirmdays}{Days for CDP confirmation (default 180).}

\item{baselinewindow}{Days from diagnosis for baseline EDSS (default 730).}

\item{rebaselinerelapse}{Logical; if TRUE, reset baseline after last relapse
(default FALSE).}
}
\value{
A list with \verb{$data} (data.table with one row per person containing
\code{pira_date} and \code{raw_date}) and \verb{$info} containing N_cdp, N_pira, N_raw,
windowbefore, windowafter, confirmdays, baselinewindow.
}
\description{
Identifies confirmed disability progression events and classifies them as
PIRA (outside relapse windows) or RAW (relapse-associated worsening).
}
\examples{
edss <- data.table::data.table(
  pid = rep("A", 4),
  edss = c(2.0, 3.5, 3.5, 3.5),
  date = as.Date(c("2010-01-01", "2011-06-01", "2011-09-01", "2012-01-01")),
  dx = as.Date("2009-06-01")
)
rel <- data.table::data.table(
  pid = "A",
  relapse_date = as.Date("2010-06-01")
)
pira(edss, idvar = "pid", edssvar = "edss", datevar = "date",
     dxdate = "dx", relapses = rel, relapseidvar = "pid")
}
