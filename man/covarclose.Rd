% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covarclose.R
\name{covarclose}
\alias{covarclose}
\title{Extract closest covariate values to an index date}
\usage{
covarclose(
  master,
  using,
  idvar,
  indexdate,
  datevar,
  vars,
  prefer = "closest",
  yearformat = FALSE,
  impute = FALSE,
  missing_codes = NULL
)
}
\arguments{
\item{master}{A data.frame or data.table with ID and index date columns.}

\item{using}{A data.frame or data.table with longitudinal covariate data.}

\item{idvar}{Name of the ID column (must exist in both datasets).}

\item{indexdate}{Name of the index date column in master (Date class).}

\item{datevar}{Name of the date column in the covariate file.}

\item{vars}{Character vector of covariate column names to extract.}

\item{prefer}{Preference for timing: \code{"closest"} (default), \code{"before"}, or \code{"after"}.}

\item{yearformat}{Logical; if TRUE, treat datevar as a year and convert to
July 1 of that year.}

\item{impute}{Logical; if TRUE, fill missing values using forward then backward
fill within person before selecting closest.}

\item{missing_codes}{Numeric vector of values to treat as missing during imputation.}
}
\value{
A list with \verb{$data} (master data.table with extracted covariate columns
merged) and \verb{$info} containing n_total, vars, prefer.
}
\description{
From longitudinal covariate data (e.g., LISA), extracts the observation
closest to each person's index date. Supports preference for before/after/closest
and optional forward/backward fill imputation.
}
\examples{
master <- data.table::data.table(
  id = c("A", "B"),
  index_dt = as.Date(c("2015-06-01", "2016-01-15"))
)
covar_data <- data.table::data.table(
  id = rep(c("A", "B"), each = 3),
  date = as.Date(c("2014-01-01", "2015-01-01", "2016-01-01",
                    "2015-01-01", "2016-01-01", "2017-01-01")),
  income = c(300, 320, 340, 400, 420, 440)
)
covarclose(master, covar_data, idvar = "id", indexdate = "index_dt",
           datevar = "date", vars = "income")
}
