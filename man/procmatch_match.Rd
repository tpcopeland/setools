% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procmatch.R
\name{procmatch_match}
\alias{procmatch_match}
\title{Match procedure codes across procedure variables}
\usage{
procmatch_match(dt, codes, procvars, prefix = FALSE)
}
\arguments{
\item{dt}{A data.frame or data.table with procedure code columns.}

\item{codes}{Character vector of procedure codes (or space/comma-separated string).}

\item{procvars}{Character vector of column names containing procedure codes.}

\item{prefix}{Logical; if TRUE, use prefix matching (startsWith) instead of exact match.}
}
\value{
A list with \verb{$data} (data.table with \code{proc_match} column) and
\verb{$info} (n_codes, n_matches, codes).
}
\description{
Generates a binary indicator for rows where any procedure variable matches
the specified KVA codes. Supports exact and prefix matching.
}
\examples{
dt <- data.table::data.table(
  id = 1:3,
  op1 = c("AB123", "CD456", "AB123"),
  op2 = c(NA, "EF789", NA)
)
procmatch_match(dt, codes = "AB123", procvars = c("op1", "op2"))
}
