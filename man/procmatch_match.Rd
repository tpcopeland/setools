% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/procmatch.R
\name{procmatch_match}
\alias{procmatch_match}
\title{Match procedure codes across procedure variables}
\usage{
procmatch_match(dt, codes, procvars, prefix = FALSE)
}
\arguments{
\item{dt}{A data.frame or data.table with procedure code columns.}

\item{codes}{Character vector of procedure codes to match. Also accepts a
single space-separated or comma-separated string (e.g., \code{"ABC10 DEF20"}
or \code{"ABC10, DEF20"}).}

\item{procvars}{Character vector of column names containing procedure codes
to search across.}

\item{prefix}{Logical; if TRUE, use prefix matching (\code{startsWith}) instead
of exact match. Default FALSE.}
}
\value{
A list with two elements:
\describe{
\item{\verb{$data}}{The input data.table with a \code{proc_match} column added
(1 = matched, 0 = not matched).}
\item{\verb{$info}}{A list with: \code{varname} ("proc_match"), \code{codes} (normalized
code vector), \code{n_codes}, \code{n_matches} (number of rows matching).}
}
}
\description{
Generates a binary indicator for rows where any of the procedure code
columns matches the specified codes. Supports exact matching and prefix
(starts-with) matching. Codes are compared case-insensitively.
}
\details{
Procedure code data in Swedish registries typically has multiple procedure
columns per row (e.g., \code{op1} through \code{op20}). This function checks all
specified columns and sets \code{proc_match = 1} if any column matches any of
the target codes.

Codes can be provided as a character vector, a single space-separated
string, or a comma-separated string. All codes are normalized to uppercase
before matching.

With \code{prefix = TRUE}, matching uses \code{startsWith()} so code \code{"ABC"} matches
\code{"ABC10"}, \code{"ABC20"}, etc. This is useful for matching code groups.
}
\examples{
library(data.table)

# --- Exact matching across two procedure columns ---
dt <- data.table(
  id = 1:4,
  op1 = c("ABC10", "DEF20", "ABC15", "GHI30"),
  op2 = c("XYZ99", "ABC10", NA, "DEF20")
)
res <- procmatch_match(dt, codes = "ABC10", procvars = c("op1", "op2"))
res$data$proc_match
#> [1] 1 1 0 0
# Row 1: op1 matches. Row 2: op2 matches. Rows 3-4: no match.

# --- Prefix matching: all codes starting with "ABC" ---
res2 <- procmatch_match(dt, codes = "ABC", procvars = c("op1", "op2"),
                        prefix = TRUE)
res2$data$proc_match
#> [1] 1 1 1 0
# Row 3 now matches (ABC15 starts with ABC)

# --- Multiple codes as a single string ---
res3 <- procmatch_match(dt, codes = "ABC10 DEF20",
                        procvars = c("op1", "op2"))
res3$data$proc_match
#> [1] 1 1 0 1
res3$info$n_matches  # 3

# --- Comma-separated codes ---
res4 <- procmatch_match(dt, codes = "ABC10, GHI30",
                        procvars = c("op1", "op2"))
res4$data$proc_match
#> [1] 1 1 0 1
}
\seealso{
\code{\link[=procmatch_first]{procmatch_first()}} for finding the earliest match per person
}
