% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dateparse.R
\name{dateparse_inwindow}
\alias{dateparse_inwindow}
\title{Check if dates fall within a window}
\usage{
dateparse_inwindow(dt, datevar, start, end)
}
\arguments{
\item{dt}{A data.frame or data.table.}

\item{datevar}{Name of the date column to check (Date class).}

\item{start}{Either a column name in \code{dt} containing per-row start dates, or
a date string for a fixed window start (parsed via \code{\link[=dateparse_parse]{dateparse_parse()}}).}

\item{end}{Either a column name in \code{dt} containing per-row end dates, or a
date string for a fixed window end.}
}
\value{
A list with two elements:
\describe{
\item{\verb{$data}}{The input data.table with \code{in_window} column added
(logical: TRUE/FALSE).}
\item{\verb{$info}}{A list with \code{n_inwindow} (count of TRUE rows).}
}
}
\description{
Adds a logical indicator column (\code{in_window}) for each row where the date
variable falls within the range \code{[start, end]} (inclusive on both ends).
}
\examples{
library(data.table)

# --- Fixed window (date strings) ---
dt <- data.table(
  id = 1:4,
  visit = as.Date(c("2019-12-01", "2020-03-01", "2020-07-15", "2021-01-10"))
)
res <- dateparse_inwindow(dt, "visit", "2020-01-01", "2020-12-31")
res$data$in_window
#> [1] FALSE  TRUE  TRUE FALSE
res$info$n_inwindow  # 2

# --- Per-row window (column names) ---
dt2 <- data.table(
  id = 1:3,
  visit = as.Date(c("2020-03-01", "2020-07-15", "2021-01-10")),
  win_start = as.Date(c("2020-01-01", "2020-06-01", "2020-01-01")),
  win_end = as.Date(c("2020-06-30", "2020-12-31", "2020-12-31"))
)
res2 <- dateparse_inwindow(dt2, "visit", "win_start", "win_end")
res2$data$in_window
#> [1]  TRUE  TRUE FALSE
}
\seealso{
\code{\link[=dateparse_window]{dateparse_window()}} for creating window columns
}
